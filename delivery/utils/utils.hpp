#ifndef UTILS_HPP
#define UTILS_HPP

#include <vector>
#include <string>

bool isNumber(const std::string &s);
std::vector<std::string> split(const std::string& str, char delimiter);

#endif // UTILS_HPP
